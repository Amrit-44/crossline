# Frontend (Next.js). Leave unset for local dev (uses http://127.0.0.1:8787);
# set to the deployed realtime Worker URL in production.
NEXT_PUBLIC_REALTIME_URL=

# Public site URL used for metadata/links (production).
NEXT_PUBLIC_SITE_URL=https://cross-line.pages.dev
